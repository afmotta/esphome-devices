# ============================================================================
# Locale Tecnico
# Ground Floor
# ============================================================================
# Heating: None
# Cooling: Fancoil
# ============================================================================
defaults:
  room_slug: "locale_tecnico"
  room_name: "Locale Tecnico"
  temperature_sensor: locale_tecnico_temp_abstracted

packages:
  sensors: !include
    file: ../../room_sensors.yaml
    vars:
      ha_temperature_sensor_id: "sensor.room_locale_tecnico_temperature"
      ha_humidity_sensor_id: "sensor.room_locale_tecnico_humidity"

  fancoil: !include
    file: ../../packages/components/fancoil.yaml
    vars:
      output_id: analog_output_5

  autotune_fancoil: !include
    file: ../../packages/components/pid_autotune.yaml
    vars:
      pid_id: locale_tecnico_fancoil_pid
      pid_name: "Locale Tecnico Fancoil"
