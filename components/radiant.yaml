packages:
  pid: !include
    file: heat_only_radiant.yaml

climate:
  - id: !extend pid_radiant_${room_slug}
    cool_output: radiant_output_${room_slug}

# Summer mode handling is now centralized in components/seasonal_mode.yaml
# and the device-level apply_hp_mode_to_all_pids script.
