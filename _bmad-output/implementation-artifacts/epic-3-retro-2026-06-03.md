---
epic: 3
date: 2026-06-03
mode: full-retrospective
project: canbus
---

# Epic 3 Retrospective — Gateway Firmware: Home Assistant Integration

## Party Mode Opening

Amelia (Developer): Epic 3 detected complete — 3 of 3 stories `done`, retrospective key `optional`. Full retro, no-blame, systems-first.
John (PM): Framing up front — 3.2 and 3.3 were verification + light hardening of handlers that already existed from the Epic 2 follow-up, not greenfield. Judge "done" accordingly.
Winston (Architect): And at close, Story 3.3's implementation is uncommitted in the working tree and `epic-3` still reads `in-progress`. Same status-hygiene miss as Epic 2.
Alberto (Project Lead): Proceed with Epic 3.

## Epic Review Summary

- Epic: Epic 3, Gateway Firmware — Home Assistant Integration
- Stories: 3 of 3 done (3.1 base config TWAI/esp-idf/API, 3.2 CAT_INPUT → HA events, 3.3 CAT_STATUS heartbeat logging)
- Implemented by: claude-opus-4-8 (dev), one code review per story
- FRs/NFRs covered: FR-6.x (HA events), FR-7.2 (secrets), NFR-1/2/4/8
- Character of the epic: verification + hardening of pre-existing handlers + the first-ever gateway compile gate

## Delivery and Quality Snapshot

- Compile gate PASS on ESPHome 2026.5.0 with both `on_frame` handlers present (RAM 14.1%, Flash 9.5%).
- Code review per story applied 4 real hardening patches; ~6 items deferred (mostly doc/pre-existing).
- Hardware/runtime verification DEFERRED entirely to Epic 4 (no bench): AC9 of 3.2 (button → HA event) and live-heartbeat of 3.3.
- Status-hygiene defect at close (recurring): Story 3.3 implementation uncommitted (`firmware/gateway.yaml`, story doc, `deferred-work.md`), `epic-3` still `in-progress` in sprint-status.

## What Went Well

- **Epic 2's prep sprint paid off measurably.** The three Epic-2 critical-path items — esp-idf gateway compile, `${output_can_id}` direction convention, click-timing dead-zones — were genuinely closed before Epic 3 (commits `a5ee1f2`, `3d7830a`). First epic where prep produced visible smoothness in execution.
- **Protocol-header SSoT discipline held a third epic.** All gateway lambdas decode via named helpers (`payload_*`, `event_type_str`, `ERR_NONE`, `CAN_FRAME_SIZE`, `PROTO_V1`). No magic numbers or raw indices.
- **`if:`/`condition:` guard pattern codified and consistent** across both `on_frame` entries — size guard first, exact bound, no `x.empty()`/magic `8`.
- **Code review caught genuine correctness issues:** 3.2 added the `MSG_BUTTON_EVENT` message-type guard and an unknown-event-type skip (logs but doesn't forward garbage to HA); 3.3 unified two divergent heartbeat log formats.
- **A deliberate, documented architecture deviation:** kept the `homeassistant.event:` YAML action over architecture.md's mandated internal-API single-lambda call (Alberto, 2026-06-02), recorded in memory and story Decision Notes, with explicit "do not refactor back" guidance for future reviewers.

## Challenges and Friction Points

1. **Story-3.1 latent defect surfaced at Story 3.2's compile gate.** 3.1 shipped `api: password:` — removed in ESPHome 2026.1.0 — and was marked `done` with an AC6 "ready for compilation" that was never actually run (project-context.md:79 had warned the gateway was never compiled). 3.2 absorbed the fix (migrated to `api: encryption: key:`). A "done" story shipped uncompilable code; the next story paid the debt.
2. **Status hygiene recurred for the THIRD consecutive epic.** Epic 1 AI#1 (close status drift) → Epic 2 AI#2 (make it a mechanical gate) → Epic 3 close: 3.3 uncommitted, `epic-3` still `in-progress`. The Epic 2 takeaway ("technical guardrails stick, process guardrails don't") proved correct again.
3. **Deferred ledger now contains self-contradictions.** project-context.md (lines 64, 152) mandates staging-globals — "always stage values into globals first" — while the shipped 3.2 handler and AC6 deliberately use per-field re-decode with no globals. The canonical context doc contradicts shipped code. Also deferred: no rate-limit/dedup on HA event firing, README pinned-version not enforced (`min_version:` absent), format-specifier UB.

## Pattern Analysis Across Story Records

### Net-new deferred items (Epic 3)

- project-context.md staging-globals mandate contradicts shipped no-globals CAT_INPUT handler [project-context.md:64,152]
- No rate-limit/dedup on `homeassistant.event:` firing — noisy/stuck frame floods HA event bus [gateway.yaml:97]
- README pinned ESPHome version is doc-only; no `esphome: min_version:` enforcement; migration note omits HA re-adoption with the new encryption key [README.md:67-74]
- `%u`/`%02u` format specifiers receive `uint8_t` promoted to signed `int` — formally UB, harmless here, codebase-wide [gateway.yaml:124-128]

### Carried/structural

- architecture.md still mandates the internal-API single-lambda call we deviated from — documented but uncorrected divergence between planning artifact and firmware.

### Breakthrough / Reusable

- API password→encryption migration recovery was clean: key generated, placeholdered in `secrets.yaml.example`, real key gitignored in `secrets.yaml`.
- `if:`/`condition:` guard + per-field re-decode is now the standard gateway `on_frame` shape.

## Previous Retrospective Continuity (Epic 2 follow-through)

| # | Item | Owner | Status |
|---|---|---|---|
| 1 | Commit Story 2.3 + complete Dev Record | Amelia | ✅ Done (`11497d7`) |
| 2 | **Status hygiene as a mechanical gate** | John | ❌ NOT ADDRESSED — recurred (3.3 uncommitted at close) |
| 3 | Resolve `${output_can_id}` direction convention | Winston | ✅ Done + effective (Epic 3 filters clean) |
| 4 | Recompile gateway under esp-idf | Amelia | ✅ Done (`a5ee1f2`) + re-validated at 3.2 gate |
| 5 | Triage click-timing dead-zones | John+Amelia | ✅ Done — windows tightened; de-risks Epic 4 matrix |
| 6 | Confirm/record pinned ESPHome version | John | ✅ Done (README → 2026.5.0); ⚠️ not enforced in YAML |

Takeaway: 5 of 6 closed and the technical ones were *effective* — Epic 3 ran smoothly because of them. The single pure-process item (#2) failed a third time. The split is now structural, not coincidental: technical guardrails set as code stick; process guardrails set as prose evaporate. **Resolution this epic: make the status gate mechanical (tooling), not aspirational (doc).**

## Next Epic Preparation — Epic 4: PoC Validation & Sign-off

Scope: 4.1 bench assembly + commissioning; 4.2 node-100 acceptance matrix; 4.3 node-101 matrix + PoC sign-off. Full 10-combination matrix (5 event types × 2 nodes), artifacts under `validation/`, sign-off in `firmware/VALIDATION.md`.

Dependencies / risks on Epic 3 work:

- **The gateway has never run on hardware.** Every Epic 3 AC was compile-gate + code-review. AC9 (3.2) and live-heartbeat (3.3) were deferred into Epic 4. Epic 4.1 is now load-bearing for 100% of the gateway's runtime behavior — the `on_frame` → `homeassistant.event` chain fires for real for the first time on the bench.
- **API auth changed mid-epic (`password:` → `encryption: key:`).** The README migration note doesn't mention that a freshly-adopted gateway must be paired in HA *with the encryption key*. Epic 4.1 is the first real HA adoption — add this step to commissioning now.
- De-risked: click-timing dead-zones (Epic 2 follow-up) closed, so 4.2/4.3 double-click and extra-long-press are safer.
- Epic 4 plan itself remains valid — no plan-breaking discovery. Risk is concentration of runtime risk + one undocumented adoption step.

## Action Items

### Decisions taken by Alberto in this retro

- **Status hygiene → mechanical hook/script gate** (not another aspirational action item).
- **Epic 4.1 flagged load-bearing + add HA re-adoption (encryption-key pairing) step** to its commissioning checklist.
- **Doc-correction pass before Epic 4** — amend project-context.md and architecture.md to match shipped firmware.

### Process

1. **Build a mechanical "close-story" gate.**
   - Owner: Amelia (Developer)
   - Done when: a script or pre-commit/CI check blocks marking a story `done` unless (a) code is committed, (b) compile gate passed, (c) sprint-status updated. Status hygiene is enforced by tooling, not prose.

2. **Mark compile-gate ACs as not-satisfiable without an actual compile.**
   - Owner: John (PM)
   - Done when: a story with a "compiles successfully" AC cannot be closed on "ready for compilation" — the compile must have run (prevents the 3.1-style latent defect).

### Technical

3. **Commit the pending Story 3.3 working-tree changes and flip epic-3 → done.**
   - Owner: Amelia (Developer)
   - Done when: `firmware/gateway.yaml`, the 3.3 story doc, and `deferred-work.md` are committed; sprint-status reflects `epic-3: done`. (Immediate critical-path item — see below.)

### Documentation (pre-Epic-4 doc-correction pass)

4. **Correct project-context.md to match shipped firmware.**
   - Owner: Paige (Tech Writer) / Amelia
   - Done when: lines 64 and 152 no longer mandate staging-globals; they describe the shipped per-field re-decode (no globals) and link the [[project_gateway_ha_event_firing_approach]] decision.

5. **Amend architecture.md to record the `homeassistant.event:` reversal.**
   - Owner: Winston (Architect)
   - Done when: architecture.md no longer mandates the internal-API single-lambda call / lists the YAML action as an anti-pattern; the PoC decision is recorded so planning artifact and firmware agree.

6. **Add the HA encryption-key re-adoption step to Epic 4.1 commissioning.**
   - Owner: John (PM)
   - Done when: Story 4.1 notes that the gateway must be (re-)paired in HA with `api_encryption_key`; optionally add `esphome: min_version: 2026.5.0` to enforce the pinned version.

## Critical Path Before Epic 4

1. **Commit Story 3.3 + flip epic-3 → done** (Action #3) — Owner: Amelia. *Immediate.*
2. **Doc-correction pass** (project-context.md + architecture.md, Actions #4/#5) — Owner: Paige/Winston.
3. **Build the close-story mechanical gate** (Action #1) — Owner: Amelia. *Before 4.1 close, so Epic 4 is the first epic that can't drift.*

## Significant Change Detection

- **Plan-breaking discoveries for Epic 4: none.** Epic 4's plan remains valid.
- **Risk concentration flagged:** Epic 4.1 carries all gateway runtime risk (first hardware execution of the full handler chain). Mitigation: treat 4.1 as load-bearing, add the HA encryption-key re-adoption step, optionally add a minimal "boots + connects to HA + logs one frame" smoke check before the full matrix.

## Readiness Assessment

- **Testing & quality:** Compile gate green (both handlers). Zero runtime verification — by design, deferred to Epic 4.1. Acceptable for a pre-hardware PoC, but 4.1 is now the single proof point.
- **Deployment & acceptance:** N/A for production; PoC acceptance is the Epic 4 matrix + sign-off.
- **Technical health:** Gateway firmware SSoT-clean and well-guarded. Risks: (a) growing deferred ledger now self-contradictory at the doc level, (b) status-hygiene gate still not mechanical until Action #1 lands, (c) un-run hardware.
- **Verdict:** Epic 3 is functionally complete and compiles. NOT clean to fully close until the immediate critical-path item (commit 3.3 + flip epic-3 → done) is done. The doc-correction pass and the mechanical gate should land before Epic 4.1 closes.

## Final Commitments

- Action items agreed: 6
- Critical-path items agreed: 3
- Significant discovery flagged: 1 (Epic 4.1 concentrates all gateway runtime risk)
- Decisions taken: mechanical status gate; flag 4.1 + HA re-adoption step; doc-correction pass before Epic 4
- Preparation focus for Epic 4: mechanical close-story gate, doc/firmware reconciliation, bench-side HA re-adoption step
