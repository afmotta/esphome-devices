# Completed / Resolved Work

Archive of resolved deferred-work items, recorded decisions, and closed follow-ups. **Pending
items live in [deferred-work.md](deferred-work.md).** Grouped by source; within each group,
the wording is preserved from when the item was closed.

---

## Resolved from: spec-adr-0009-central-map-binding-manifest.md

- **RESOLVED 2026-06-14 — Export contract + full generator pipeline (ADR-0009 §4/§7)** [`firmware/tools/generate_nodes.py`, `firmware/protocol/bindings.h`, `firmware/registry/map.json`, `firmware/gateway/ha_manifest_package.yaml`] — `generate_nodes.py` now emits, from one validated run: (a) `registry/map.json`, the read-only export (`schema_version`, `manifest_hash`, `nodes[]` of node_id/floor/room/board/location/sensors, §7); (b) the full compiled `BINDINGS[]` table + `struct BindingEntry` + `binding_find()` in `bindings.h` (frozen-additive, §4 — empty today, fallback still log-only); (c) the **generated** `ha_manifest_package.yaml` readiness-heartbeat package with the hash baked in, retiring the hand-paste into `ha_arbitration_automations.yaml` (which keeps only the still-log-only ACK automation). **Two decisions (Alberto):** §7's "generation timestamp" is a *deterministic* `map_version` content digest, **not** wall-clock — unchanged input regenerates byte-for-byte (no diff churn), preserving the kept slice's zero-diff property; and ADR-0009 open item 3 is resolved as a **heartbeat-only** generated package (ACK stays hand-maintained until bindings are manifest-derived). Native test `firmware/tests/test_generate_exports.py`; gateway compiles SUCCESS with the table; protocol header untouched. _map.json field shape stays provisional — open item 5 (confirm with HVAC firmware) remains pending._
- **RESOLVED 2026-06-14 — Controller drift-visibility entities (ADR-0009 §6)** [`firmware/gateway/gateway.yaml`, `firmware/tools/generate_nodes.py`, `firmware/protocol/node_map.h`, `firmware/tests/test_generate_exports.py`] — the gateway now exposes two read-only HA diagnostic `text_sensor`s: **Binding Manifest Hash** (compiled `BINDINGS_MANIFEST_HASH`) and **Node Map Version** (`NODE_MAP_VERSION`). Both are compile-time constants published once from `on_boot` (`update_interval: never`, no polling). The generator gained `NODE_MAP_VERSION` in `node_map.h`, set to the **same** deterministic `map_version` it stamps into `registry/map.json` (no wall-clock `BINDINGS_GENERATED_AT`, per the export-slice decision) — so a dashboard comparison against the committed `map.json` is verbatim and "committed but not yet reflashed" drift shows there instead of as a misbehaving button. To compute `map_version` before emitting `node_map.h`, `write_exports` now runs before `write_node_map` in `main()`, which also means an invalid `bindings.yaml` aborts before `node_map.h` is rewritten (a side-improvement to the validate-before-persist item — `nodes/*.yaml` ordering is unchanged). Two native tests added (`test_node_map_emits_version_constant`, `test_generator_node_map_version_matches_map_json`); gateway compiles SUCCESS (esphome 2026.5.0). **Implementer call (confirm with Alberto):** `NODE_MAP_VERSION` reuses `map.json`'s `map_version`, which digests the full registry map (incl. floor/sensors), not just the rows compiled into the C struct. Consequence: a floor-only edit rolls the version and shows drift even though gateway behavior is unchanged — over-signalling that errs toward a harmless gateway reflash (it has OTA), the safe direction, and keeps the dashboard comparison a verbatim string match against `map.json`. If under-signalling is preferred, digest only the compiled rows instead.
- **RESOLVED 2026-06-14 — HA package generation details (ADR-0009 open item 3)** [`firmware/gateway/ha_manifest_package.yaml`] — decided by the §4 slice above: a **single generated package file** carrying the readiness-heartbeat automation with the hash baked in (not a blueprint, not split automations). The ACK automation stays hand-maintained in `ha_arbitration_automations.yaml` until bindings are manifest-derived. No further work — this was a decision, now recorded.
- **RESOLVED 2026-06-14 — Push-discipline gate (ADR-0009 open item 4)** [`firmware/tools/check_registry_pushed.py`, `firmware/tests/test_push_gate.py`, spec: `spec-adr-0009-push-discipline-gate.md`] [echoes [[project_status_hygiene_mechanical_gate]]] — stdlib-only pre-reflash CLI: fails (exit 1) unless the guarded paths (`registry/` system of record + the controller-compiled artifacts `node_map.h`/`bindings.h`/`ha_manifest_package.yaml`) are clean *and* `HEAD` is on a remote (`git rev-list HEAD --not --remotes` empty). Clean is path-scoped (unrelated dirty files don't block a reflash); pushed is whole-commit (git's backup unit), and with no remote it correctly fails. Wired into the README reflash workflow as the explicit gate; exit codes (0/1/2 git-error) suit a future pre-commit/CI hook. 6 native tests against throwaway git repos. **Deliberate non-goal:** registry↔artifact *freshness* — a stale compiled map is already surfaced at runtime by the §6 drift-visibility entities; this gate is about *backup*, not staleness.
- **RESOLVED 2026-06-14 — Generator validates the binding manifest after writing node artifacts** [`firmware/tools/generate_nodes.py` `main`] — `main()` now stages node configs in memory (`node_files`) and writes them only after `write_exports` validates the manifest, so an invalid `bindings.yaml` aborts (`sys.exit(1)`) before `nodes/` or `node_map.h` is touched (validate-before-persist). Covered by `test_generator_aborts_before_writing_node_files` (drives `main()` against a temp `ROOT`). _The sibling `commission.py` atomicity item (separate file) is still pending — see [deferred-work.md](deferred-work.md)._
- **RESOLVED 2026-06-14 — `bindings.py` does not range-check `button`** [`firmware/tools/bindings.py` `validate`] — `validate()` now rejects a `button` outside the standard 0–7 set (`BUTTON_MAX = 7`), turning a silently dead binding into a generator error. Covered by `test_validation_button_out_of_range`.

## Resolved from: code review of spec-commission-interactive-cli.md (2026-06-09)

- **RESOLVED 2026-06-09 — `(room, board)` uniqueness now enforced in the generator** [`firmware/tools/generate_nodes.py`] — `generate_nodes.main()` now rejects two commissioned nodes sharing the same `(room, board)` with a clear error + `sys.exit(1)`, and also range-checks room/board against `ROOM_BOARD_MAX` (255, matching `commission.py` `MAX_RB`). The `(0, 0)` placeholder that `allocate_node.py` seeds is **exempt**, so multiple not-yet-commissioned nodes still coexist. Enforcing at generation (the single chokepoint for both `commission.py` paths and any hand-edit) means `commission.py` itself needs no duplicate check — it surfaces the generator's `SystemExit`. `project-context.md`'s claim that the generator validates room/board ranges + globally-unique `(room, board)` is now accurate. _Original finding: validation existed nowhere; surfaced by the interactive-CLI review, not caused by it._

## Recorded decision (won't-fix) from: code review of 3-2-gateway-cat-input-handler (2026-06-02)

- **No rate-limit/dedup on HA event firing** [`firmware/gateway.yaml:97`] — `homeassistant.event:` fires on every matching CAT_INPUT frame; a noisy bus or a stuck/repeating frame floods the HA event bus. Low risk for human-paced button clicks; pre-existing design, not introduced by Story 3.2. **Reviewed 2026-06-03 — do NOT add a gateway throttle/dedup.** Buttons are momentary ([[feedback_momentary_buttons_no_state]]): each gesture is one node-side `on_multi_click` event = one frame, and legitimate rapid distinct events (two single-clicks, different buttons) must all fire — a content-blind throttle would drop them. The real risk is a stuck/repeating frame, which is a hardware/bus fault better *surfaced* (heartbeat error flags) than silently muted. The fault-surfacing thread folds into the command-reliability story (still pending — see [deferred-work.md](deferred-work.md)) as fault detection, not rate-limiting.

## Closed During Epic 3 Deferred-Work Follow-Up (2026-06-03)

- **project-context.md staging-globals mandate corrected** [`_bmad-output/project-context.md:61-64,150-153`] — both the `homeassistant.event` data rule and the "key non-obvious facts" note claimed values must be staged into globals first. Shipped CAT_INPUT handler (`firmware/gateway.yaml:102-108`) uses a per-field `!lambda` re-decoding directly from frame vector `x` — no globals. Doc now describes the actual per-field re-decode pattern, narrows the real constraint (cannot reference a var from a *separate preceding* `lambda:` action), and records it as the deliberate [[project_gateway_ha_event_firing_approach]] decision.
- **README ESPHome version reframed as known-good, not pinned + HA re-adoption note added** [`firmware/README.md:68-83`] — "Pinned version" relabeled "Known-good version" with an explicit "Not enforced (no `esphome: min_version:`)" caveat; password→encryption migration note now states an already-adopted gateway must be deleted and re-added in HA with the new encryption key.
- **Secrets onboarding documented** [`firmware/README.md`] — new "Onboarding: `secrets.yaml`" section lists the three required keys (`wifi_ssid`, `wifi_password`, `api_encryption_key`) and states the build fails at config load if any is missing (no pre-flight validation; ESPHome default).
- **Format-specifier signed/unsigned UB fixed across the codebase** [`firmware/gateway.yaml:94-96,124-129`, `firmware/common/base_node.yaml:60`] — `uint8_t`/`uint16_t` varargs promote to signed `int`, but `%u`/`%02X` require `unsigned int` (formally UB). All affected call sites now cast each narrow arg to `(unsigned)`: the CAT_INPUT button log, both heartbeat logs (normal + error path), and the node config-write log. `%03X` with `can_id()` (`uint32_t`) left as-is — `uint32_t` is already `unsigned int`, no promotion. Verified: `esphome compile gateway.yaml` and `nodes/node100.yaml` both SUCCESS on 2026.5.0.
- **`uptime_h` overflow fully retired via payload reorg + 16-bit field + millis()-rollover counter** [`firmware/common/canbus_protocol.h`, `firmware/common/base_node.yaml`, `firmware/gateway.yaml`, `prd.md`] — superseded the interim 1-byte saturation. Two-layer overflow addressed: (1) the field is now a little-endian `uint16` (bytes 5-6, ~7.5 yr) and (2) `millis()`'s ~49.7-day uint32 wrap is tracked by a `uptime_rollovers` global incremented whenever a 30s heartbeat sees `millis()` decrease; uptime saturates at 65535 h. Enabled by a **payload-layout redesign** (see below).
- **Payload layout unified (node→gateway frames)** [`firmware/common/canbus_protocol.h`, `firmware/gateway.yaml`, `firmware/common/base_node.yaml`, `prd.md`] — both frames now share a `[version][type][room][board]` prefix with message content trailing toward the reserved tail. Button = `[ver,type,room,board,button,event,0,0]`; heartbeat = `[ver,type,room,board,errors,uptime_lo,uptime_hi,0]`. `room=d[2]`/`board=d[3]` decoders are now shared (deleted `payload_heartbeat_room/board`); `payload_uptime` → `payload_uptime16`. The PRD byte-3 "button states bitmask" was **removed** (momentary buttons have no state). `PROTO_V1` unchanged (PoC, lock-step reflash). PRD wire-spec tables + FR-6.1/6.2 byte offsets amended (correct-course). Verified: `esphome compile gateway.yaml` and `nodes/node100.yaml` both SUCCESS on 2026.5.0.
- **PRD FR-6.4 staging-globals mandate corrected at the source** [`prd.md`] — FR-6.4 was the upstream origin of the project-context staging-globals myth (closed above); rewritten to describe the actual per-field `!lambda` re-decode and note globals are not required. Closes the root of the [[project_gateway_ha_event_firing_approach]] doc drift.
- **`can_int_pin` dead substitution removed** [`firmware/generate_nodes.py`, `firmware/nodes/*`] — verified the ESPHome `mcp2515` component schema (clock/mode/SPI only) and header expose no interrupt-pin option; the component polls over SPI. `can_int_pin: "GPIO11"` was generated into every node but consumed by nothing (`base_node.yaml` never referenced it). Removed from the generator template, replaced with a comment so it is not re-added by reflex; the GPIO11 hardware INT fact stays in README OQ-2. Regenerated node100/node101 — both `esphome compile` SUCCESS on 2026.5.0. _(Also closes the 2-1 review's "`can_int_pin` generated but not consumed" item.)_

## Closed During Epic 2 Deferred-Work Follow-Up (2026-06-02)

- **Click-timing dead-zones fixed** [`firmware/common/button.yaml`] — double/triple inter-press windows tightened from `OFF for 0.05s to 0.4s` to `0.05s to 0.29s` so they no longer overlap single-click's `OFF for at least 0.3s`; long press capped at `1s to 2999ms` so the extra-long (`>= 3s`) pattern is reachable. De-risks the Epic 4 acceptance matrix for double-click and extra-long-press.
- **`send_data()` TX failures now logged** [`firmware/common/button.yaml`] — the shared `btn*_send_event` script captures the `canbus::Error` return and emits `ESP_LOGW` on non-OK, so dropped events on a busy bus are observable (NFR-8 spirit).
- **CAN ID direction convention resolved + documented** [`firmware/common/base_node.yaml`, `firmware/generate_nodes.py`, `firmware/nodes/*`] — platform-level (default TX) `can_id` now uses `${input_can_id}` (CAT_INPUT, node → gateway) instead of `${output_can_id}`; the `on_frame` RX filter keeps `${output_can_id}` (CAT_OUTPUT, gateway → node) with an explicit comment. `input_can_id` is regenerated into node files (no longer stale/unused). Direction convention is documented in the base-node header and generator node comment.
- **`(x[3] << 8)` integer promotion fixed** [`firmware/common/base_node.yaml`] — now `(static_cast<uint16_t>(x[3]) << 8) | x[4]`; portable across `int` widths.
- **`CAN_FRAME_SIZE` retyped to `std::size_t`** [`firmware/common/canbus_protocol.h`] — exact comparison against `std::vector::size()` with no implicit promotion; added `#include <cstddef>`.
- Validation: `esphome compile nodes/node100.yaml` and `node101.yaml` both SUCCESS on ESPHome 2026.5.0.

## Closed During Epic 1 Deferred-Work Follow-Up (2026-06-01)

- **Heartbeat room/board decoding fixed** [`firmware/common/canbus_protocol.h`, `firmware/gateway.yaml`] — added `payload_heartbeat_room()` / `payload_heartbeat_board()` and switched the gateway heartbeat handler to use them.
- **Header-only constants promoted to `inline constexpr`** [`firmware/common/canbus_protocol.h`] — removes the internal-linkage concern from the previous `static const` definitions while preserving the approved wire values.
- **Generator now rejects duplicate GPIOs in a row** [`firmware/generate_nodes.py`] — rows such as `"20,20"` now fail with a clear error and non-zero exit.
- **Missing-CSV bootstrap updated and made non-destructive** [`firmware/generate_nodes.py`] — the script now seeds current PoC rows (`100` / `101`, GPIO20/21) and exits before generating node YAMLs.
- **Pinned schematic references recorded** [`firmware/README.md`] — README citations now point to commit `fdefed9e521864c67a05bf19a4e5046428b86662` instead of `main`.
- **`can_clock: "16MHZ"` validated** [`firmware/nodes/node100.yaml`] — `esphome compile nodes/node100.yaml` succeeded with the current spelling.
- **Generated node INT pin audit passed** [`firmware/nodes/node100.yaml`, `firmware/nodes/node101.yaml`] — current generated nodes already use `can_int_pin: "GPIO11"`.
- **Include-path concern not reproducible** [`firmware/common/base_node.yaml`, `firmware/gateway.yaml`] — `esphome config` succeeded both from repo root (`firmware/nodes/node100.yaml`) and from `firmware/` (`nodes/node100.yaml`).
- **`MSG_BUTTON_EVENT` / `MSG_HEARTBEAT` sharing `0x01` remains intentional** [`firmware/common/canbus_protocol.h`] — confirmed by the PRD wire spec; category bits disambiguate button vs heartbeat frames.
- **`assert`-based validation note was stale** [`firmware/generate_nodes.py`] — generator validation already used explicit `print + sys.exit(1)` checks before this follow-up.
- **SPI pin corrections are already propagated downstream** [`firmware/generate_nodes.py`, `firmware/nodes/node100.yaml`, `firmware/nodes/node101.yaml`] — template and generated nodes now carry GPIO2 / GPIO3 / GPIO4 and GPIO11.
