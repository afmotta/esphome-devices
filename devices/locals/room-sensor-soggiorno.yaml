# Example ESP32 Room Sensor Configuration - Soggiorno (Living Room)
# This file demonstrates how to use the ESP32 room sensor template

substitutions:
  wifi_ssid: !secret wifi_ssid
  wifi_password: !secret wifi_password
  encryption_key: !secret encryption_key
  ota_password: !secret ota_password
  # external_components local paths resolve against THIS file's directory
  # (one level deeper than devices/) — see boards/s1-pro-multi-sense.yaml
  s1_pro_libs_path: ../../libs

packages:
  - !include "../room-sensor-soggiorno.yaml"
