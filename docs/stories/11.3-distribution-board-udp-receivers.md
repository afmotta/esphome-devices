# Story 11.3: Distribution Board UDP Receivers

**Epic:** 11 - ESP32 Room Sensors via UDP  
**Story ID:** 11.3  
**Created:** November 24, 2025  
**Status:** Not Started

---

## Status

Not Started

---

## Story

**As a** system operator,  
**I want** distribution boards to receive UDP room sensor broadcasts and integrate with the 3-tier failover component,  
**so that** room PIDs can access UDP temperature/humidity data with automatic HA fallback.

---

## Acceptance Criteria

1. ✅ Ground floor distribution board (`distribuzione-piano-terra.yaml`) updated with UDP receivers
2. ✅ First floor distribution board (`distribuzione-primo-piano.yaml`) updated with UDP receivers
3. ✅ packet_transport providers configured per room sensor (14 total: 6 ground + 8 first)
4. ✅ UDP receiver sensors created for temperature per room
5. ✅ UDP receiver sensors created for humidity per room (optional for MVP, required for Epic 12)
6. ✅ Provider naming convention: `{zone_slug}_room_sensor` (e.g., `soggiorno_room_sensor`)
7. ✅ UDP sensor IDs follow convention: `{zone_slug}_udp_temp`, `{zone_slug}_udp_humidity`
8. ✅ UDP sensors marked `internal: true` (not exposed to HA directly)
9. ✅ Room components updated to include sensor_failover_3tier with UDP mode
10. ✅ room_sensors.yaml v6 supports `use_udp` flag (default: false for backward compatibility)
11. ✅ When use_udp=true, room component delegates to sensor_failover_3tier.yaml
12. ✅ When use_udp=false, room component uses Epic 5 HA-only behavior
13. ✅ All device files compile successfully (flash <80%, RAM <15%)
14. ✅ UDP receivers work with ESP32 broadcasts from Story 11.2 template

---

## Tasks / Subtasks

- [ ] **Task 1: Update Ground Floor Distribution Board** (AC: 1, 3, 4, 5, 6, 7, 8)
  - [ ] Edit `devices/distribuzione-piano-terra.yaml`
  - [ ] Add packet_transport section with providers array
  - [ ] Add 6 providers: soggiorno, cucina, bagno, anticamera, sala_pranzo, locale_tecnico
  - [ ] Create UDP receiver sensors for temperature (6 rooms)
  - [ ] Create UDP receiver sensors for humidity (6 rooms)
  - [ ] Mark all UDP sensors `internal: true`
  - [ ] Follow naming convention for provider and sensor IDs

- [ ] **Task 2: Update First Floor Distribution Board** (AC: 2, 3, 4, 5, 6, 7, 8)
  - [ ] Edit `devices/distribuzione-primo-piano.yaml`
  - [ ] Add packet_transport section with providers array
  - [ ] Add 8 providers: bagno_grande, camera_1 through camera_6, corridoio
  - [ ] Create UDP receiver sensors for temperature (8 rooms)
  - [ ] Create UDP receiver sensors for humidity (8 rooms)
  - [ ] Mark all UDP sensors `internal: true`

- [ ] **Task 3: Update room_sensors.yaml Component** (AC: 10, 11, 12)
  - [ ] Edit `components/room_sensors.yaml` (v5 → v6)
  - [ ] Add optional var: `use_udp` (default: false)
  - [ ] Add optional var: `udp_sensor_id` (required when use_udp=true)
  - [ ] When use_udp=true: include sensor_failover_3tier.yaml with vars
  - [ ] When use_udp=false: preserve existing Epic 5 HA-only behavior
  - [ ] Update component header documentation with UDP mode usage

- [ ] **Task 4: Update Room Includes (Ground Floor)** (AC: 9)
  - [ ] Update room_control_soggiorno include in distribuzione-piano-terra.yaml
  - [ ] Add vars: `use_udp: false` (start with HA-only, enable in Story 11.5)
  - [ ] Add vars: `udp_sensor_id: "soggiorno_udp_temp"`
  - [ ] Repeat for all 6 ground floor rooms

- [ ] **Task 5: Update Room Includes (First Floor)** (AC: 9)
  - [ ] Update room_control includes in distribuzione-primo-piano.yaml
  - [ ] Add vars: `use_udp: false` (enable after pilot validation)
  - [ ] Add vars: `udp_sensor_id` for each room
  - [ ] Repeat for all 8 first floor rooms

- [ ] **Task 6: Compilation and Validation** (AC: 13, 14)
  - [ ] Compile ground floor distribution board
  - [ ] Compile first floor distribution board
  - [ ] Verify flash usage <80% (check Epic 9 baseline + UDP receivers)
  - [ ] Verify RAM usage <15%
  - [ ] Test UDP receiver with dummy ESP32 broadcasts (Story 11.2)
  - [ ] Verify sensor_failover_3tier integration (abstracted sensor accessible)

---

## Dev Notes

### Relevant Source Tree

- **Modify:** `devices/distribuzione-piano-terra.yaml` (add UDP receivers)
- **Modify:** `devices/distribuzione-primo-piano.yaml` (add UDP receivers)
- **Modify:** `components/room_sensors.yaml` (v5 → v6 with UDP mode)
- **Reference:** `components/sensor_failover_3tier.yaml` (Story 11.1)
- **Reference:** `templates/esp32-room-sensor.yaml` (Story 11.2 broadcast IDs)

### packet_transport Provider Pattern

**Distribution Board YAML Structure:**

```yaml
# UDP Receiver Setup (add to device file)
udp:
  id: udp_packet_transport

packet_transport:
  providers:
    # Ground Floor Rooms (6)
    - soggiorno_room_sensor
    - cucina_room_sensor
    - bagno_room_sensor
    - anticamera_room_sensor
    - sala_pranzo_room_sensor
    - locale_tecnico_room_sensor
    
    # First Floor Rooms (8)
    - bagno_grande_room_sensor
    - camera_1_room_sensor
    - camera_2_room_sensor
    - camera_3_room_sensor
    - camera_4_room_sensor
    - camera_5_room_sensor
    - camera_6_room_sensor
    - corridoio_room_sensor

# UDP Receiver Sensors (add to sensor section)
sensor:
  # Soggiorno UDP Receivers
  - platform: packet_transport
    id: soggiorno_udp_temp
    provider: soggiorno_room_sensor
    internal: true
    
  - platform: packet_transport
    id: soggiorno_udp_humidity
    provider: soggiorno_room_sensor
    internal: true
  
  # Repeat for all 14 rooms (28 sensors total: 14 temp + 14 humidity)
```

### room_sensors.yaml v6 UDP Mode

**Component Interface (backward compatible):**

```yaml
# v5 Behavior (Epic 5 HA-only, default)
sensors: !include
  file: ../../room_sensors.yaml
  vars:
    zone_slug: "soggiorno"
    zone_name: "Soggiorno"
    ha_temperature_sensor_id: "sensor.room_soggiorno_temperature"
    # use_udp defaults to false → Epic 5 behavior preserved

# v6 Behavior (Epic 11 UDP with HA fallback)
sensors: !include
  file: ../../room_sensors.yaml
  vars:
    zone_slug: "soggiorno"
    zone_name: "Soggiorno"
    use_udp: true
    udp_sensor_id: "soggiorno_udp_temp"
    ha_temperature_sensor_id: "sensor.room_soggiorno_temperature"
```

**Component Implementation (room_sensors.yaml):**

```yaml
# components/room_sensors.yaml v6
defaults:
  use_udp: false  # Backward compatibility: default to Epic 5 HA-only

# Conditional delegation based on use_udp flag
{% if use_udp %}
  # Epic 11 UDP Mode: Delegate to 3-tier failover
  sensor_failover: !include
    file: sensor_failover_3tier.yaml
    vars:
      zone_slug: ${zone_slug}
      zone_name: ${zone_name}
      udp_sensor_id: ${udp_sensor_id}
      ha_temperature_sensor_id: ${ha_temperature_sensor_id}
      emergency_timeout: 180
{% else %}
  # Epic 5 HA-Only Mode: Direct HA sensor (existing behavior)
  sensor:
    - platform: homeassistant
      id: ${zone_slug}_ha_temperature
      entity_id: ${ha_temperature_sensor_id}
      # ... rest of Epic 5 config
{% endif %}
```

### Room Include Pattern Update

**Before (Epic 5):**
```yaml
# devices/distribuzione-piano-terra.yaml
room_control_soggiorno: !include
  file: ../components/rooms/room_control.yaml
  vars:
    zone_slug: "soggiorno"
    zone_name: "Soggiorno"
    # ... other vars
```

**After (Epic 11, UDP disabled by default):**
```yaml
room_control_soggiorno: !include
  file: ../components/rooms/room_control.yaml
  vars:
    zone_slug: "soggiorno"
    zone_name: "Soggiorno"
    use_udp: false  # Start with HA-only, enable in Story 11.5 pilot
    udp_sensor_id: "soggiorno_udp_temp"  # Pre-configured for future enable
    # ... other vars
```

**After Pilot Validation (Story 11.5):**
```yaml
    use_udp: true  # Enable UDP mode after pilot proves stable
```

### Ground Floor Room List (6 Rooms)

| Zone Slug      | Zone Name      | Provider ID                | UDP Temp ID             | UDP Humidity ID             |
| -------------- | -------------- | -------------------------- | ----------------------- | --------------------------- |
| soggiorno      | Soggiorno      | soggiorno_room_sensor      | soggiorno_udp_temp      | soggiorno_udp_humidity      |
| cucina         | Cucina         | cucina_room_sensor         | cucina_udp_temp         | cucina_udp_humidity         |
| bagno          | Bagno          | bagno_room_sensor          | bagno_udp_temp          | bagno_udp_humidity          |
| anticamera     | Anticamera     | anticamera_room_sensor     | anticamera_udp_temp     | anticamera_udp_humidity     |
| sala_pranzo    | Sala Pranzo    | sala_pranzo_room_sensor    | sala_pranzo_udp_temp    | sala_pranzo_udp_humidity    |
| locale_tecnico | Locale Tecnico | locale_tecnico_room_sensor | locale_tecnico_udp_temp | locale_tecnico_udp_humidity |

### First Floor Room List (8 Rooms)

| Zone Slug    | Zone Name    | Provider ID              | UDP Temp ID           | UDP Humidity ID           |
| ------------ | ------------ | ------------------------ | --------------------- | ------------------------- |
| bagno_grande | Bagno Grande | bagno_grande_room_sensor | bagno_grande_udp_temp | bagno_grande_udp_humidity |
| camera_1     | Camera 1     | camera_1_room_sensor     | camera_1_udp_temp     | camera_1_udp_humidity     |
| camera_2     | Camera 2     | camera_2_room_sensor     | camera_2_udp_temp     | camera_2_udp_humidity     |
| camera_3     | Camera 3     | camera_3_room_sensor     | camera_3_udp_temp     | camera_3_udp_humidity     |
| camera_4     | Camera 4     | camera_4_room_sensor     | camera_4_udp_temp     | camera_4_udp_humidity     |
| camera_5     | Camera 5     | camera_5_room_sensor     | camera_5_udp_temp     | camera_5_udp_humidity     |
| camera_6     | Camera 6     | camera_6_room_sensor     | camera_6_udp_temp     | camera_6_udp_humidity     |
| corridoio    | Corridoio    | corridoio_room_sensor    | corridoio_udp_temp    | corridoio_udp_humidity    |

### Flash/RAM Budget Analysis

**Epic 9 Baseline (Supply Temp UDP):**
- Flash: ~67% (mixing group + supply temp broadcasts)
- RAM: ~12% (UDP packet buffers)

**Epic 11 Addition (14 Room Receivers):**
- 14 providers × ~200 bytes = 2.8 KB flash
- 14 temp sensors × ~300 bytes = 4.2 KB flash
- 14 humidity sensors × ~300 bytes = 4.2 KB flash
- UDP packet buffers: 14 × 64 bytes = 896 bytes RAM
- **Total overhead:** ~11.2 KB flash, ~1 KB RAM

**Estimated Final Usage:**
- Flash: ~70-72% (well below 80% limit)
- RAM: ~13-14% (well below 15% limit)

### Testing Checklist

**UDP Receiver Validation:**
- [ ] Distribution board receives UDP broadcasts from ESP32 (tcpdump verification)
- [ ] UDP sensor IDs match broadcast IDs from Story 11.2 template
- [ ] Packet rate: ~2 packets per 10 seconds per room sensor (temp + humidity)
- [ ] Sensor values update within 1 second of broadcast

**Failover Integration:**
- [ ] With use_udp=false: room uses HA sensor (Epic 5 behavior preserved)
- [ ] With use_udp=true: room uses sensor_failover_3tier (UDP → HA → NaN)
- [ ] Abstracted sensor ID matches expected pattern: `{zone_slug}_room_temp_abstracted`
- [ ] PID controller references abstracted sensor correctly

**Compilation:**
- [ ] Ground floor board compiles: flash <80%, RAM <15%, zero errors/warnings
- [ ] First floor board compiles: flash <80%, RAM <15%, zero errors/warnings

---

## Dependencies

**Requires:**
- Story 11.1: sensor_failover_3tier component (integration target)
- Story 11.2: ESP32 room sensor template (broadcast ID contract)

**Enables:**
- Story 11.5: Pilot room deployment (needs UDP receivers ready)
- Story 11.6: Batch migration (needs receivers for all rooms)

---

## Estimates

**Effort:** 3 story points (~2 days)

**Breakdown:**
- Ground floor distribution board updates: 3 hours
- First floor distribution board updates: 3 hours
- room_sensors.yaml v6 UDP mode: 2 hours
- Room include updates (14 rooms): 1 hour
- Compilation validation: 1 hour
- UDP receiver testing (with dummy ESP32): 1 hour
- Documentation updates: 1 hour

**Elapsed Time:** 2 days (sequential dev work)

---

## Acceptance

**Definition of Done:**
- Both distribution boards compile successfully
- UDP receivers configured for all 14 rooms
- room_sensors.yaml v6 supports use_udp flag with backward compatibility
- All rooms have `use_udp: false` (enabled incrementally in Story 11.5/11.6)
- UDP receiver tested with Story 11.2 ESP32 broadcasts (dummy validation)
- Ready for Story 11.5 pilot room deployment

---

**Story Status:** Not Started

**Agent:** (Assign)  
**Date:** November 24, 2025
