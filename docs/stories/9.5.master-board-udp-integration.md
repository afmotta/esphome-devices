# Story 9.5: Master Board UDP Integration

**Epic:** 9 - UDP Packet Transport Board Communication  
**Story ID:** 9.5  
**Created:** November 19, 2025  
**Status:** OBSOLETE

---

## Status

OBSOLETE - Work already completed in Story 9.1

---

## Obsolescence Rationale

This story proposed integrating UDP broadcasting into the master board (`devices/gruppo-miscelazione.yaml`). However, **this work was already completed in Story 9.1**.

**What Story 9.1 Already Implemented:**
✅ UDP configuration added to `devices/gruppo-miscelazione.yaml`
- `udp:` section with ID `udp_packet_transport` (line 19)
- `packet_transport:` section with platform: udp (lines 21-29)
✅ Both Dallas supply temperature sensors broadcasting:
- `dallas_0x81000000b3e6f628` → `mixing_output_ground_floor_temperature`
- `dallas_0xe9000000b366ed28` → `mixing_output_first_floor_temperature`
✅ 10-second update interval configured
✅ Inline implementation (no separate component file needed)
✅ Firmware compilation validated (Story 9.1 Task 3 complete)
✅ Functional regression tested (Story 9.1 Task 4 complete)

**Why This Story Became Redundant:**
1. **Story 9.1 used inline configuration** - No separate `udp_sender.yaml` component was created (native ESPHome `packet_transport` is sufficient)
2. **Simpler approach prevailed** - Direct inline UDP config in device file is cleaner than package abstraction
3. **All functionality delivered** - Broadcasting supply temperatures is the core Epic 9 requirement, already complete

**What This Story Proposed But Wasn't Needed:**
- ❌ **Task 1**: Add UDP configuration → Already in gruppo-miscelazione.yaml
- ❌ **Task 2**: Climate mode broadcasting → Not needed for Epic 9 MVP (supply temps sufficient)
- ❌ **Task 3**: Compile/flash testing → Already done in Story 9.1
- ⚠️ **Task 4**: Network validation → Requires physical hardware (Story 9.1 Task 5 incomplete)
- ❌ **Task 5**: Functional regression → Already done in Story 9.1
- ⚠️ **Task 6**: Diagnostics → Story 9.4 marked obsolete (native diagnostics sufficient)

**Current State:**
The master board is **already broadcasting UDP packets** with both supply temperatures. Slave boards can receive via native `packet_transport` sensor platform (demonstrated in Story 9.3 test configuration).

**If Future Work Needed:**
- Climate mode broadcasting: Create focused story if requirement emerges
- Network validation: Physical hardware testing (continuation of Story 9.1 Task 5)

---

## Story

**As a** system administrator,  
**I want** the master board (gruppo-miscelazione) to broadcast supply temperatures and climate mode via UDP,  
**so that** slave boards can receive this critical data directly without relying on Home Assistant as an intermediary.

---

## Acceptance Criteria

1. `devices/gruppo-miscelazione.yaml` integrates `udp_sender.yaml` component via packages
2. UDP sender broadcasts both Dallas supply temperature sensors (piano terra + primo piano)
3. UDP sender includes climate mode text sensor in broadcasts
4. Feature flag `use_udp: true` enables UDP broadcasting
5. Board compiles successfully with UDP sender included
6. Firmware size remains within ESP32 flash limits after UDP addition
7. UDP packets visible on network (Wireshark/tcpdump) containing supply temps
8. Existing functionality unchanged (PID controllers, mixing valves, HA integration)
9. UDP diagnostic sensors visible in HA dashboard
10. Rollback capability validated by setting `use_udp: false`

---

## Tasks / Subtasks

- [ ] **Task 1: Add UDP Configuration to gruppo-miscelazione** (AC: 1, 2)
  - [ ] Add `udp:` section with ID to device file
  - [ ] Add `packet_transport:` section with platform: udp
  - [ ] Map both Dallas sensor IDs to broadcast_ids
  - [ ] Set appropriate update_interval (10s default)

- [ ] **Task 2: Climate Mode Broadcasting (Optional)** (AC: 3)
  - [ ] Determine if climate mode broadcasting is needed for Epic 9 MVP
  - [ ] If needed, research adding text_sensor support to packet_transport
  - [ ] Create text_sensor that derives mode from PID states if required

- [ ] **Task 3: Compile and Flash Testing** (AC: 5, 6)
  - [ ] Run `esphome compile devices/gruppo-miscelazione.yaml`
  - [ ] Verify firmware binary size is within limits
  - [ ] Flash firmware to gruppo-miscelazione board via OTA
  - [ ] Monitor ESPHome logs for UDP initialization messages

- [ ] **Task 4: Network Validation** (AC: 7)
  - [ ] Set up network packet capture (Wireshark/tcpdump on port 6053)
  - [ ] Verify UDP packets appear every 10 seconds
  - [ ] Validate JSON payload contains both supply temps and climate mode
  - [ ] Check timestamp values are reasonable

- [ ] **Task 5: Functional Regression Testing** (AC: 8)
  - [ ] Verify both mixing valve PIDs still operate normally
  - [ ] Check HA dashboard shows all entities (no missing sensors)
  - [ ] Test mixing valve control (heating/cooling transitions)
  - [ ] Monitor for any unexpected errors in ESPHome logs

- [ ] **Task 6: Diagnostics and Rollback Validation** (AC: 9, 10)
  - [ ] Check HA dashboard for `sensor.udp_packets_sent` counter
  - [ ] Verify counter increments every 10 seconds
  - [ ] Test rollback: set `use_udp: false`, recompile, flash
  - [ ] Confirm UDP packets stop when feature flag disabled
  - [ ] Verify system continues normal operation with UDP disabled

---

## Dev Notes

### Relevant Source Tree

**Target Device File:**
- `devices/gruppo-miscelazione.yaml` (KC868-A6 master board)

**Component Dependencies:**
- `components/udp_sender.yaml` (created in Story 9.1)
- `boards/a6.yaml` (hardware config with Ethernet/WiFi)
- `boards/wifi.yaml` (network connectivity)

**Existing Sensor IDs (from gruppo-miscelazione.yaml):**
- Dallas supply temp piano terra: `dallas_0x81000000b3e6f628`
- Dallas supply temp primo piano: `dallas_0xe9000000b366ed28`
- PID controllers: `pid_mixing_ground_floor`, `pid_mixing_first_floor`

### Climate Mode Sensor Strategy

The master board needs to broadcast the current climate mode (heating/cooling) to slave boards. Options:

**Option 1: Use existing HA sensor** (if already consumed)
```yaml
sensor:
  - platform: homeassistant
    id: master_climate_mode
    entity_id: sensor.thermostat_mode
```

**Option 2: Derive from PID states** (autonomous)
```yaml
text_sensor:
  - platform: template
    id: master_climate_mode
    name: "Master Climate Mode"
    lambda: |-
      if (id(pid_mixing_ground_floor).mode == climate::CLIMATE_MODE_HEAT) {
        return {"heating"};
      } else if (id(pid_mixing_ground_floor).mode == climate::CLIMATE_MODE_COOL) {
        return {"cooling"};
      } else {
        return {"off"};
      }
```

**Recommendation:** Use Option 2 for true autonomy (no HA dependency for climate mode).

### Integration Pattern

Add to `devices/gruppo-miscelazione.yaml`:
```yaml
packages:
  base: !include ../boards/a6.yaml
  wifi: !include ../boards/wifi.yaml
  # ... existing packages ...
  
  # UDP Sender (Epic 9)
  udp_sender: !include
    file: ../components/udp_sender.yaml
    vars:
      use_udp: true
      sensors:
        - dallas_0x81000000b3e6f628
        - dallas_0xe9000000b366ed28
      text_sensors:
        - master_climate_mode
      update_interval: 10
      broadcast_port: 6053
```

### Firmware Size Considerations

Current gruppo-miscelazione firmware includes:
- 2 PID controllers with sensor monitoring components
- Dallas temperature sensors (1-Wire)
- Ethernet + WiFi networking
- PCF8574 I2C expanders

Adding UDP sender will increase firmware by ~5-10KB (JSON serialization + UDP stack). Monitor binary size during compile:
```bash
esphome compile devices/gruppo-miscelazione.yaml | grep "RAM:"
```

### Network Configuration

- Master board has static IP or DHCP reservation (verify in `boards/wifi.yaml`)
- UDP broadcasts to `xxx.xxx.xxx.255` (broadcast address for /24 network)
- Port 6053 (ESPHome default, no conflicts expected)

### Rollback Strategy

If UDP causes issues:
1. Set `use_udp: false` in gruppo-miscelazione.yaml
2. Recompile and flash via OTA
3. System reverts to Epic 8 behavior (HA-only coordination)
4. No data loss, no manual intervention on slave boards needed

### Previous Epic Context

**Epic 8:** Unified state machine with coordinator
- Master board currently only manages mixing valves
- Slave boards consume supply temps from HA `homeassistant` platform sensors
- Epic 9 will make supply temps available via UDP, but HA sensors remain as Tier 2 fallback

### Testing Standards

#### Testing Requirements
1. **Compile Test:** Firmware builds without errors
2. **Size Test:** Binary fits in flash (<1MB available)
3. **Network Test:** UDP packets visible on network capture
4. **Regression Test:** All existing PID/mixing valve functionality works
5. **Diagnostic Test:** UDP counters visible in HA dashboard
6. **Rollback Test:** Feature flag disables UDP cleanly

#### Testing Procedure
```bash
# Compile and check size
esphome compile devices/gruppo-miscelazione.yaml

# Flash via OTA
esphome upload devices/gruppo-miscelazione.yaml --device gruppo-miscelazione.local

# Monitor logs
esphome logs devices/gruppo-miscelazione.yaml --device gruppo-miscelazione.local

# Network capture (separate machine)
sudo tcpdump -i eth0 -n udp port 6053 -A

# Validate JSON payload
tcpdump -i eth0 -n udp port 6053 -w /tmp/udp.pcap
tshark -r /tmp/udp.pcap -T fields -e data.text
```

---

## Change Log

| Date       | Version | Description                                   | Author      |
| ---------- | ------- | --------------------------------------------- | ----------- |
| 2025-11-19 | 1.0     | Initial story creation                        | Sarah (PO)  |
| 2025-11-20 | 1.1     | Marked OBSOLETE - Work completed in Story 9.1 | James (Dev) |
