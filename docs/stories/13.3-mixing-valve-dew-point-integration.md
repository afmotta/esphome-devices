# Story 13.3: Mixing Valve Dew Point Integration

**Epic:** 13 - Ground Floor Hybrid Radiant-Fancoil Cooling  
**Date:** December 26, 2025  
**Status:** Ready for Review  
**Story Points:** 2  
**Priority:** High (Safety Critical)

---

## User Story

As a **system owner enabling radiant cooling**,  
I want **the mixing valve to automatically limit supply water temperature based on dew point**,  
So that **condensation on floors is prevented during cooling operations**.

---

## Story Context

**Safety-Critical Feature:** This story implements the core condensation prevention mechanism. Supply water temperature must never drop below dew point + safety margin.

**Existing Pattern (First Floor):**
```yaml
# From components/rooms/first_floor/first-floor.yaml
sensor:
  - platform: combination
    id: first_floor_max_dew_point
    ...
    on_value:
      then:
        - climate.control:
            id: pid_mixing_valve_first_floor_radiant
            target_temperature: !lambda "return (x + ${safe_margin});"
```

**Ground Floor Mixing Valve:**
- Component: `mixing_pump_ground_floor_radiant` in `ground-floor.yaml`
- PID ID: `pid_mixing_valve_ground_floor_radiant`
- Controls supply water temperature via analog output

---

## Acceptance Criteria

### Functional Requirements

1. **Dew Point → Supply Temp Linkage:**
   - [x] `ground_floor_max_dew_point` updates trigger supply temp adjustment
   - [x] Supply temp minimum = `max_dew_point + safe_margin`
   - [x] Default `safe_margin` = 1.0°C (configurable)

2. **Mixing Valve Control:**
   - [x] `on_value` trigger on `ground_floor_max_dew_point` sensor
   - [x] `climate.control` updates `pid_mixing_valve_ground_floor_radiant` target
   - [x] Target = dew point + safe margin during cooling

3. **Mode Awareness:**
   - [x] Dew point limiting only applies during cooling mode (summer_mode check)
   - [x] Heating mode uses normal supply temp targets (no update when summer_mode=false)
   - [x] Uses `summer_mode` binary sensor for cooling vs heating detection

### Safety Requirements

4. **Always-On Protection:**
   - [x] Dew point protection cannot be disabled accidentally (hardcoded in on_value)
   - [x] Protection active even if coordinator fails (independent of coordinator)
   - [x] Mixing valve respects limit regardless of zone states

5. **Failover Behavior:**
   - [x] If dew point sensors unavailable → combination sensor returns NaN → no target update
   - [x] Conservative approach: no update means mixing valve uses last safe target
   - [x] Document: Failover relies on sensor layer (room_sensors.yaml provides 3-tier failover)

### Technical Requirements

6. **Variable Configuration:**
   - [x] `safe_margin` as configurable default (1.0°C) in ground-floor.yaml defaults
   - [x] Can be adjusted per-floor if needed (same pattern as first floor)

7. **Compilation & Validation:**
   - [x] `esphome compile` succeeds
   - [x] No circular dependencies
   - [x] Supply temp sensor shows reasonable values (verified in config)

---

## Technical Implementation

### Files to Modify

| File                                              | Change                                         |
| ------------------------------------------------- | ---------------------------------------------- |
| `components/rooms/ground_floor/ground-floor.yaml` | Add `on_value` trigger to max dew point sensor |

### Implementation Pattern

**Update ground-floor.yaml:**
```yaml
defaults:
  safe_margin: 1.0  # °C above dew point for safety

sensor:
  - platform: combination
    id: ground_floor_max_dew_point
    name: "Max Dew Point Piano Terra"
    type: max
    unit_of_measurement: "°C"
    device_class: "temperature"
    state_class: "measurement"
    sources:
      - source: soggiorno_dew_point
      - source: cucina_dew_point
      - source: anticamera_dew_point
    on_value:
      then:
        - if:
            condition:
              # Only during cooling mode
              lambda: 'return id(summer_mode).state;'
            then:
              - climate.control:
                  id: pid_mixing_valve_ground_floor_radiant
                  target_temperature: !lambda "return (x + ${safe_margin});"
              - logger.log:
                  format: "Dew point limiting: supply temp min = %.1f°C"
                  args: ['x + ${safe_margin}']
```

### Mode Detection

**Question:** How is cooling mode currently detected?
- Option A: `binary_sensor.summer_mode` exists
- Option B: Check `thermostat_mode` text sensor
- Option C: Create new mode detection logic

**First Floor Pattern Check:**
```yaml
# Check if first floor has mode-aware dew point limiting
# If not, ground floor should match pattern
```

---

## Dependencies

| Dependency                            | Status   | Notes                                 |
| ------------------------------------- | -------- | ------------------------------------- |
| Story 13.2 (Dew Point Infrastructure) | Required | Provides `ground_floor_max_dew_point` |
| `mixing_pump_ground_floor_radiant`    | ✅ Exists | Target for supply temp control        |
| Summer/cooling mode detection         | ❓ Verify | Need to check existing pattern        |

---

## Risks & Mitigations

| Risk                     | Likelihood | Impact | Mitigation                                       |
| ------------------------ | ---------- | ------ | ------------------------------------------------ |
| Dew point sensor failure | Medium     | High   | Conservative fallback (Story 13.2 documents)     |
| Mode detection incorrect | Medium     | Medium | Test thoroughly; heating shouldn't trigger limit |
| Safe margin too small    | Low        | High   | Start with 1.0°C; industry standard is 2.0°C     |
| Supply temp oscillation  | Low        | Medium | PID should handle smoothly                       |

---

## Open Questions

1. **Mode Detection:** Does `summer_mode` binary sensor exist and work correctly?
2. **Safe Margin Value:** 1.0°C vs 2.0°C - which is appropriate for this installation?
3. **Heating Interaction:** Should there be any dew point awareness during heating? (Probably not)
4. **Failover Strategy:** Disable cooling or use conservative limit when sensors fail?

---

## Testing Checklist

### Pre-Deployment
- [ ] Verify `summer_mode` or equivalent exists
- [ ] Review first floor implementation for consistency
- [ ] Confirm mixing valve PID ID

### Post-Deployment
- [ ] In cooling mode: verify supply temp respects dew point limit
- [ ] In heating mode: verify dew point limiting doesn't interfere
- [ ] Simulate high dew point (humid conditions) → supply temp increases
- [ ] Simulate sensor failure → verify failover behavior

### Safety Validation
- [ ] Supply temp NEVER drops below dew point + margin during any test
- [ ] Mode transitions don't cause temporary limit violations

---

## Definition of Done

- [x] `on_value` trigger wired to mixing valve
- [x] Mode-aware (cooling only via summer_mode check)
- [x] Safe margin configurable (defaults: safe_margin: 1.0)
- [x] Failover behavior documented (NaN → no update → last safe target)
- [ ] Testing confirms supply temp limit enforced (requires deployment)
- [x] Ready for Story 13.5 (coordinator component)

---

## Dev Agent Record

### Agent Model Used
Claude Opus 4.5

### File List
- `components/rooms/ground_floor/ground-floor.yaml` - Modified: Added mode-aware (summer_mode) condition to dew point on_value trigger with logging

### Debug Log References
- Initial logger.log args error: EFloat substitution not supported, simplified to single arg format

### Completion Notes
1. Added `summer_mode` condition check to `on_value` trigger - dew point limiting only applies during cooling mode
2. Added logger.log for debugging dew point protection activation
3. Safe margin configurable via `defaults: safe_margin: 1.0` (already existed from Story 13.2)
4. Failover: If dew point sensors unavailable → combination sensor returns NaN → no target update → mixing valve uses last safe value
5. Implementation matches first floor pattern but with added mode awareness for ground floor

### Change Log
| Date       | Change                                          | Reason                        |
| ---------- | ----------------------------------------------- | ----------------------------- |
| 2025-12-27 | Added summer_mode condition to on_value trigger | Mode-aware dew point limiting |
| 2025-12-27 | Added logger.log for dew point protection       | Debugging and monitoring      |

---

## Notes

- This is the PRIMARY safety mechanism against condensation
- First floor pattern provides proven reference
- Epic 12 describes more sophisticated dew point protection if needed
- Safe margin can be tuned after initial deployment
