# Story 6.1: MEV Device Configuration and Base Setup

**Epic:** 6 - MEV Board Integration  
**Story Points:** 5  
**Priority:** HIGH (Construction Timeline Critical)  
**Status:** Draft

---

## User Story

**As a** homeowner  
**I want** a KC868-A6 board configured for MEV control  
**So that** I can expose MEV hardware controls to Home Assistant

---

## Description

Create the foundational device configuration for the MEV board, including base board setup, device-level assembly, and local/remote configuration files. This establishes the hardware abstraction layer following the Epic 5 architecture pattern (HA-driven control, exposed entities only).

---

## Acceptance Criteria

### Board Configuration
- [ ] Device file `devices/mev-primo-piano.yaml` created
- [ ] Includes base board package from `boards/a6.yaml` 
- [ ] WiFi/Ethernet connectivity configured
- [ ] Device name: "MEV Primo Piano"
- [ ] Friendly device description in ESPHome

### Local Configuration
- [ ] File `locals/mev-primo-piano.yaml` created
- [ ] WiFi credentials referenced from `locals/secrets.yaml`
- [ ] API encryption enabled
- [ ] OTA password configured
- [ ] Device-specific substitutions defined

### Remote Configuration
- [ ] File `remotes/mev-primo-piano.yaml` created (stub for future remote config)
- [ ] Follows existing remote config pattern from other devices

### Compilation & Resource Usage
- [ ] ESPHome config compiles without errors
- [ ] Flash usage < 60%
- [ ] RAM usage < 15%
- [ ] No compilation warnings related to device config

### Basic Connectivity Test
- [ ] Board can be flashed via USB initially
- [ ] Board connects to WiFi/Ethernet successfully
- [ ] Board appears in Home Assistant ESPHome integration
- [ ] OTA updates work correctly

---

## Technical Implementation Notes

### File Structure
```
devices/
  mev-primo-piano.yaml      # Main device assembly
locals/
  mev-primo-piano.yaml      # Secrets and local substitutions
remotes/
  mev-primo-piano.yaml      # Remote configuration support
```

### Device Configuration Pattern
```yaml
# devices/mev-primo-piano.yaml
substitutions:
  device_name: mev-primo-piano
  friendly_name: MEV Primo Piano
  device_description: "First Floor Mechanical Extract Ventilation Control"

packages:
  base: !include ../boards/a6.yaml
  wifi: !include ../boards/wifi.yaml
  mev: !include ../components/mev.yaml
    vars:
      mev_slug: "mev_primo_piano"
      mev_name: "MEV Primo Piano"
```

---

## Dependencies

- KC868-A6 board hardware available
- Story 6.2 depends on this foundation

---

## Definition of Done

- [ ] All acceptance criteria met
- [ ] Code compiles without errors
- [ ] Board successfully flashed and connected to HA
- [ ] Configuration files follow project conventions

---

**Created:** October 30, 2025
