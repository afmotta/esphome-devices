# Story 3.3: Remove Modbus Slave from distribuzione-primo-piano

## Status
Draft

## Story
**As a** system architect,  
**I want** to remove Modbus slave functionality from distribuzione-primo-piano and replace remote sensor reading with direct Home Assistant sensor integration,  
**so that** the distribution board operates independently without requiring board-to-board Modbus communication.

## Acceptance Criteria

1. Remove `modbus_slave` package from `devices/distribuzione-primo-piano.yaml`
2. Remove `modbus_slave_registers` package from `devices/distribuzione-primo-piano.yaml`
3. Replace `sensor_failover` logic with direct Home Assistant sensor subscriptions
4. Update fancoil configurations to use HA temperature sensors instead of Modbus failover
5. Verify all fancoils continue to operate correctly with HA-provided temperatures
6. Configuration validates and compiles without errors
7. No regression in fancoil temperature control or climate mode handling

## Tasks / Subtasks

- [ ] Task 1: Analyze current slave configuration (AC: 1, 2, 3)
  - [ ] Read `devices/distribuzione-primo-piano.yaml` and identify modbus_slave packages
  - [ ] Identify all sensor_failover instances and their Modbus dependencies
  - [ ] Document which fancoils use which temperature sensors
  - [ ] List Home Assistant sensor entity IDs to be used as replacements

- [ ] Task 2: Remove Modbus slave packages (AC: 1, 2)
  - [ ] Remove `modbus_slave` package reference from device file
  - [ ] Remove `modbus_slave_registers` pack3. Replace `sensor_failover` logic with direct Home Assistant sensor subscn/scripts
  - [4. Update fancoil configurations to [ ] Task 3: Replace sensor failover with HA sen5. Verify all fancoils continue to operate correctlyssistant sensor platform configuration6. Configurabscribe to Dallas sensor entities from gruppo-miscelazione via HA
  - [ ] Update fancoil PID `sensor:` references to use HA sensors
  - [ ] Remove `sensor_failover` component usage
  - [ ] Document new sensor entity mapping

- [ ] Task 4: Testing and validation (AC: 5, 6, 7)
  - [ ] Compile firmware and check for errors
  - [ ] Deploy to test environment
  - [ ] Verify fancoil PIDs receive temperature data from HA
  - [ ] Test climate mode changes (heating/cooling)
  - [ ] Monitor fancoil control stability
  - [ ] Document any behavioral changes

## Dev Notes

### Relevant Source Tree
- **Device file**: `devices/distribuzione-primo-piano.yaml`
- **Slave components** (to remove): 
  - `components/modbus_slave.yaml`
  - `components/modbus_slave_registers.yaml`
  - `components/sensor_failover.yaml`
- **Fancoil components** (to update): 
  - `components/second_floor_fancoils.yaml`
  - `components/fancoil.yaml`
- **HA Sensor Platform**: Use ESPHome `homeassistant` sensor platform

### Architecture Context
**Current Role**: distribuzione-primo-piano is a Modbus slave board (A16 Ethernet) that:
- Reads Dallas temperature sensors from gruppo-miscelazione via Modbus registers
- Uses sensor_failover to handle Modbus disconnections with local fallback sensors
- Controls 8 fancoil zones on the first floor (primo piano)

**After Epic 3.3**: distribuzione-primo-piano becomes independent board that:
- Subscribes to Dallas sensors from gruppo-miscelazione via Home Assistant
- No Modbus slave functionality
- Fancoils use HA-provided temperature sensors directly
- Home Assistant coordinates climate modes and temperatures

### Important Notes
- Epic 2 already simplified fancoil PID architecture
- Each fancoil currently has a `sensor_failover` providing temperature to its PID
- Sensor failover reads from Modbus (primary) with local Dallas (fallback)
- Need to replace failover with direct HA sensor subscription
- This is identical pattern to Story 3.2, but for first floor instead of ground floor

### Key Preservation Points
1. **Fancoil PIDs**: Keep PID configurations, only change sensor source
2. **Relay/Output Control**: No changes to fancoil relay/valve outputs
3. **ESPHome API**: Ensure enabled for bidirectional HA integration
4. **Sensor IDs**: Update to reference HA sensor entity IDs

### Home Assistant Sensor Integration Pattern
```yaml
sensor:
  - platform: homeassistant
    id: primo_piano_temperature
    entity_id: sensor.gruppo_miscelazione_dallas_primo_piano
    internal: true  # Don't re-expose to HA

climate:
  - platform: pid
    id: fancoil_zona_1
    sensor: primo_piano_temperature  # Reference HA sensor
    # ... rest of config
```

### Testing

#### Testing Standards
- **Test Location**: Manual testing in production environment
- **Validation Method**: 
  - ESPHome CLI: `esphome config devices/distribuzione-primo-piano.yaml`
  - ESPHome CLI: `esphome compile devices/distribuzione-primo-piano.yaml`
  - Runtime testing: Deploy via OTA, monitor logs and HA entities
- **Testing Focus**:
  - Configuration validation
  - Compilation success
  - Fancoil PID control with HA sensors
  - Temperature sensor availability and updates

#### Specific Tests for This Story
1. **Configuration validation**: Ensure YAML is valid after slave removal
2. **Sensor connectivity**: HA sensors provide data to fancoil PIDs
3. **PID functionality**: Fancoils control temperature correctly
4. **Mode coordination**: Climate mode changes reflected in all fancoils
5. **No slave references**: Search codebase for slave-related artifacts

## Change Log

| Date       | Version | Description                    | Author |
| ---------- | ------- | ------------------------------ | ------ |
| 2025-10-22 | 1.0     | Story created from Epic 3      | Sarah  |

## Dev Agent Record

### Agent Model Used
TBD

### Debug Log References
TBD

### Completion Notes
TBD

### File List
**To Modify:**
- `devices/distribuzione-primo-piano.yaml`
- `components/second_floor_fancoils.yaml` (if sensor references need updating)

**To Reference:**
- `components/modbus_slave.yaml` (understand what's being removed)
- `components/modbus_slave_registers.yaml` (understand register mappings)
- `components/sensor_failover.yaml` (understand current failover logic)
- `components/fancoil.yaml` (understand fancoil PID sensor requirements)

## QA Results
TBD
