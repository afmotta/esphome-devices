# Story 13.6: Zone Demand Aggregation Update

**Epic:** 13 - Ground Floor Hybrid Radiant-Fancoil Cooling  
**Date:** December 26, 2025  
**Status:** Ready  
**Story Points:** 2  
**Priority:** High

---

## User Story

As a **system owner enabling radiant cooling**,  
I want **the zone demand aggregation to include cooling mode activity**,  
So that **the mixing pump activates correctly during radiant cooling operations**.

---

## Story Context

**Current State:**
The `ground_floor_radiant_any_zone_open` binary sensor determines when to run the mixing pump. Currently it only checks heating PID activity.

**Problem:**
If radiant zones are cooling but the aggregation only checks heating activity, the mixing pump won't activate → no cold water flow → no cooling.

**Existing Code (ground-floor.yaml):**
```yaml
binary_sensor:
  - platform: template
    id: ground_floor_radiant_any_zone_open
    name: "Ground Floor Radiant Any Zone Open"
    device_class: running
    icon: "mdi:pump"
    lambda: |-
      return id(pid_radiant_anticamera_is_active) ||
             id(pid_radiant_bagno_terra_is_active) ||
             id(pid_radiant_cucina_is_active) ||
             id(pid_radiant_soggiorno_is_active);
```

**Required Change:**
Include cooling activity detection, or check PID action rather than just "is_active" which may be heating-specific.

---

## Acceptance Criteria

### Functional Requirements

1. **Cooling Mode Detection:**
   - [ ] Zone demand includes radiant cooling activity
   - [ ] Mixing pump activates when any zone is cooling
   - [ ] Works in both normal mode (PID) and boost mode (100%)

2. **Aggregation Logic Update:**
   - [ ] Check PID `action` property (HEATING, COOLING, IDLE, OFF)
   - [ ] Or check `is_active` sensor that covers both modes
   - [ ] Verify existing `pid_radiant_*_is_active` sensors definition

3. **Boost Mode Compatibility:**
   - [ ] When boost mode active (radiant at 100%), pump must run
   - [ ] Consider adding boost_mode_active to aggregation OR

4. **Pump Control:**
   - [ ] `mixing_pump_ground_floor_radiant` activates for cooling
   - [ ] No changes needed to pump relay logic (just aggregation)

### Technical Requirements

5. **Backward Compatibility:**
   - [ ] Heating mode continues to work correctly
   - [ ] No regression in winter operation

6. **Compilation:**
   - [ ] `esphome compile` succeeds
   - [ ] No circular dependencies

---

## Technical Implementation

### Option A: Check PID Action (Recommended)

Update lambda to check climate action:

```yaml
binary_sensor:
  - platform: template
    id: ground_floor_radiant_any_zone_open
    name: "Ground Floor Radiant Any Zone Open"
    device_class: running
    icon: "mdi:pump"
    lambda: |-
      // Check if any radiant zone is actively heating OR cooling
      bool anticamera_active = (id(pid_radiant_anticamera).action == CLIMATE_ACTION_HEATING ||
                                id(pid_radiant_anticamera).action == CLIMATE_ACTION_COOLING);
      bool bagno_active = (id(pid_radiant_bagno_terra).action == CLIMATE_ACTION_HEATING ||
                           id(pid_radiant_bagno_terra).action == CLIMATE_ACTION_COOLING);
      bool cucina_active = (id(pid_radiant_cucina).action == CLIMATE_ACTION_HEATING ||
                            id(pid_radiant_cucina).action == CLIMATE_ACTION_COOLING);
      bool soggiorno_active = (id(pid_radiant_soggiorno).action == CLIMATE_ACTION_HEATING ||
                               id(pid_radiant_soggiorno).action == CLIMATE_ACTION_COOLING);
      
      // Also check boost mode (radiant fully open regardless of PID)
      bool boost_active = id(boost_mode_active);
      
      return anticamera_active || bagno_active || cucina_active || soggiorno_active || boost_active;
```

### Option B: Keep Existing + Add Boost

If `pid_radiant_*_is_active` already handles both modes:

```yaml
lambda: |-
  return id(pid_radiant_anticamera_is_active) ||
         id(pid_radiant_bagno_terra_is_active) ||
         id(pid_radiant_cucina_is_active) ||
         id(pid_radiant_soggiorno_is_active) ||
         id(boost_mode_active);  // Add boost mode
```

### Investigation Needed

Check `pid_sensors.yaml` to understand what `pid_radiant_*_is_active` actually measures:
- Does it use `CLIMATE_ACTION_HEATING` only?
- Or does it check any non-IDLE/OFF action?

---

## Files to Modify

| File | Change |
|------|--------|
| `components/rooms/ground_floor/ground-floor.yaml` | Update zone demand lambda |
| `components/pid_sensors.yaml` | **Review** - understand is_active definition |

---

## Dependencies

| Dependency | Status | Notes |
|------------|--------|-------|
| Story 13.1 (Radiant Migration) | Required | PIDs must support cooling |
| Story 13.5 (Coordinator) | Required | Provides `boost_mode_active` global |
| `mixing_pump.yaml` | ✅ Exists | Uses zone demand sensor |

---

## Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Pump doesn't run during cooling | High if not fixed | High | This story is the fix |
| Heating regression | Low | High | Test heating mode after change |
| is_active sensor incompatible | Medium | Medium | Use Option A (explicit action check) |

---

## Testing Checklist

### Pre-Deployment
- [ ] Review `pid_sensors.yaml` is_active definition
- [ ] Understand current aggregation behavior
- [ ] Verify `boost_mode_active` global available

### Post-Deployment
- [ ] **Cooling test:** Enable cooling mode → verify pump runs
- [ ] **Heating test:** Enable heating mode → verify pump runs (regression)
- [ ] **Boost test:** Enter boost mode → verify pump runs
- [ ] **Idle test:** All zones idle → verify pump stops

### Edge Cases
- [ ] One zone cooling, others idle → pump runs
- [ ] Mode transition (heating → cooling) → pump continues
- [ ] Boost mode deactivates → pump controlled by PID again

---

## Definition of Done

- [ ] Zone demand includes cooling activity
- [ ] Pump activates during radiant cooling
- [ ] Boost mode included in aggregation
- [ ] Heating mode continues working (no regression)
- [ ] Ready for Story 13.7 (diagnostic sensors)

---

## Notes

- Critical for radiant cooling to actually work
- Simple change but high impact if missed
- Option A is safer (explicit action check) but Option B is simpler
