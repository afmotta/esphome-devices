# Story 6.2: MEV Component Implementation

**Epic:** 6 - MEV Board Integration  
**Story Points:** 8  
**Priority:** HIGH  
**Status:** Draft

---

## User Story

**As a** homeowner  
**I want** a reusable MEV component that exposes all hardware controls  
**So that** Home Assistant can orchestrate intelligent ventilation automation

---

## Description

Implement the reusable `components/mev.yaml` package that abstracts MEV hardware controls (4 relays, 0-10V fan speed, alarm input) as Home Assistant entities. This component follows the Epic 5 pattern: board exposes controls, HA provides intelligence.

---

## Acceptance Criteria

### Component Creation
- [ ] File `components/mev.yaml` created with parameterized vars
- [ ] Accepts vars: `mev_slug`, `mev_name`, relay IDs, DAC ID, alarm input ID
- [ ] Component can be included in device configs with `!include` and `vars`
- [ ] Follows existing component patterns (consistent with `room_sensors.yaml`, etc.)

### Relay Controls (4 Switches)
- [ ] Power switch: `switch.${mev_slug}_power`
- [ ] Winter/Summer mode switch: `switch.${mev_slug}_mode` (icon: thermometer)
- [ ] Dehumidifier switch: `switch.${mev_slug}_dehumidifier` (icon: water-percent)
- [ ] Cooling integration switch: `switch.${mev_slug}_cooling` (icon: snowflake)
- [ ] All switches have friendly names using `${mev_name}` prefix
- [ ] Switches control PCF8574 relays from `boards/a6.yaml`

### 0-10V Fan Speed Control
- [ ] Number entity: `number.${mev_slug}_fan_speed`
- [ ] Range: 0-100 (percentage)
- [ ] Maps to KC868-A6 built-in 0-10V DAC output (DAC_1 or DAC_2)
- [ ] Direct voltage control: 0%=0V, 100%=10V (linear)
- [ ] Friendly name: "${mev_name} Fan Speed"
- [ ] Icon: fan
- [ ] Step: 1 (percentage increments)

### Alarm Input Monitoring
- [ ] Binary sensor: `binary_sensor.${mev_slug}_alarm`
- [ ] Reads closed circuit = alarm active
- [ ] Device class: problem
- [ ] Friendly name: "${mev_name} Alarm"
- [ ] Proper inverted logic if needed (closed = ON)

### Device Registry
- [ ] All entities grouped under single device: "MEV Primo Piano"
- [ ] Device info includes manufacturer, model, sw_version
- [ ] Entities appear organized in HA device view

### Hardware Integration
- [ ] 0-10V conversion approach documented (op-amp or converter module)
- [ ] Relay pin assignments documented in component
- [ ] DAC output pin confirmed (GPIO25 or GPIO26)
- [ ] Alarm input pin assignment confirmed

---

## Technical Implementation Notes

### Component Interface
```yaml
# components/mev.yaml
# Expected vars:
# - mev_slug: "mev_primo_piano"
# - mev_name: "MEV Primo Piano"  
# - power_relay: output_1 (PCF8574 relay ID)
# - mode_relay: output_2
# - dehumid_relay: output_3
# - cooling_relay: output_4
# - fan_speed_dac: dac_1 (DAC output ID)
# - alarm_input: input_1 (binary sensor input)

switch:
  - platform: gpio
    name: "${mev_name} Power"
    id: "${mev_slug}_power"
    pin:
      pcf8574: outputs
      number: ${power_relay}
      
output:
  - platform: esp32_dac
    pin: GPIO25
    id: ${fan_speed_dac}
    
number:
  - platform: template
    name: "${mev_name} Fan Speed"
    id: "${mev_slug}_fan_speed"
    min_value: 0
    max_value: 100
    step: 1
    mode: slider
    lambda: "return 0;"
    set_action:
      - output.set_level:
          id: ${fan_speed_dac}
          level: !lambda 'return x / 100.0;'
```

### DAC Configuration
- KC868-A6 has built-in 0-10V DAC outputs (DAC_1/DAC_2)
- No external conversion needed
- Direct mapping: 0%→0V, 100%→10V (linear)

---

## Dependencies

### Upstream
- Story 6.1 (Device Configuration) must be complete

### Downstream  
- Story 6.3 (Documentation) needs component interface defined

### External
- MEV technical verification (relay polarity, alarm output specs)
- Review existing A6 board config for DAC output configuration

---

## Testing Strategy

### Compilation Testing
- [ ] Component compiles when included in device config
- [ ] No variable resolution errors
- [ ] Resource usage still within limits (Flash < 60%, RAM < 15%)

### Hardware Testing (Bench Test)
- [ ] All 4 relay switches toggle correctly (multimeter verification)
- [ ] Fan speed output voltage scales linearly 0-10V (multimeter verification)
- [ ] Alarm input detects closed circuit correctly
- [ ] Entity states update in HA within 1 second of command

### Integration Testing
- [ ] All entities visible in HA
- [ ] Device registry shows proper grouping
- [ ] Entities controllable from HA dashboard
- [ ] Board continues operating at last settings when HA disconnects (5 min test)

---

## Open Questions

- **Q1:** Which GPIO pins for relays? (Review a6.yaml PCF8574 mapping)
- **Q2:** Which DAC output - DAC_1 or DAC_2? (Check existing board usage)
- **Q3:** Alarm input: PCF8574 or direct GPIO? (Check available inputs)

---

## Definition of Done

- [ ] All acceptance criteria met
- [ ] Component compiles and integrates with device config
- [ ] All 6 entities operational and tested
- [ ] DAC 0-10V output verified with multimeter
- [ ] Code follows project conventions
- [ ] Peer review completed

---

**Created:** October 30, 2025
